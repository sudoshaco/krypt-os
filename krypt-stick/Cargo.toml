[package]
name = "krypt-stick"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "krypt-stick"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive", "env"] }
nix = { version = "0.29", features = ["user"] }
libc = "0.2"
